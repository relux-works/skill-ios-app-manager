# STUB — not yet wired to real project
name: Test (Stub)

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

defaults:
  run:
    working-directory: tuist-starter

jobs:
  test:
    runs-on: macos-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      - name: Setup
        run: make setup
      - name: Test
        run: make test
