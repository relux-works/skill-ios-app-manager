## Blueprint Examples

Versioned blueprint examples live here for reuse with:

```bash
ios-app-manager module create --from <path-to-blueprint.json>
```

Current catalog:

- `xflow-ios/` — relux-feature blueprints captured from the XFlow iOS migration workspace

These files are examples and starting points, not global source of truth for any live app.
