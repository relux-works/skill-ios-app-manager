public enum {{.ModuleName}} {
    public enum Data {
        public enum Api {
{{- if .HasHTTP}}
            public enum Http {}
{{- end}}
            public enum DTO {}
        }
    }
    public enum Business {
        public enum Model {}
    }
    public enum UI {
        public enum Model {}
{{- range .Features}}
        public enum {{.Name}} {}
{{- end}}
    }
}
