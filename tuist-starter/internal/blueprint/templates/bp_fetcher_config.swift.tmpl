import Foundation

extension {{.ModuleName}}.Data.Api.Http {
    public final class Config: Sendable {
        /// Base API URL for this module's endpoints.
        public let baseURL: String

        /// Scaffolded example endpoint path.
        /// Replace with your real API endpoint paths.
        public let scaffoldedEndpoint: String

        public init(
            baseURL: String = "https://api.example.com",
            scaffoldedEndpoint: String = "/scaffolded"
        ) {
            self.baseURL = baseURL
            self.scaffoldedEndpoint = baseURL + scaffoldedEndpoint
        }
    }
}
