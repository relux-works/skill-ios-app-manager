import Foundation

extension {{.ModuleName}}.Business {
    public enum Err: Error, Equatable, Sendable {
        /// Scaffolded example: network request failed.
        case networkError(String)

        /// Scaffolded example: unexpected or unknown error.
        case unknown(String)
    }
}
