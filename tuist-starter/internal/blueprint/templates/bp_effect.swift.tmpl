import Relux

extension {{.ModuleName}}.Business {
    public enum Effect: Relux.Effect {
        /// Scaffolded example: trigger a data fetch.
        /// Replace with your real effects.
        case scaffoldedEffect
    }
}
