import Relux

extension {{.ModuleName}}.Business {
    public enum Action: Relux.Action {
        /// Scaffolded example: data fetched successfully.
        /// Replace with your real actions.
        case scaffoldedSuccess(id: UUID)

        /// Scaffolded example: data fetch failed.
        case scaffoldedFail(error: {{.ModuleName}}.Business.Err)
    }
}
