Implemented tuist CLI wrapper package at internal/tuistproj with: (1) TuistRunner subprocess execution for generate/install/graph/clean/edit/dump plus version, passthrough args, working directory support, verbose realtime streaming, and structured RunError with exit code/stdout/stderr. (2) Version checker that runs tuist version, parses semantic version output, and enforces minimum major version 4.x. (3) Graph loader/parser that runs tuist graph --format json and extracts targets, dependencies, and module types from JSON. Added mock-based tests and golden graph fixture in internal/tuistproj/testdata/graph.json. Verification passed: go test ./internal/tuistproj/..., go vet ./..., go build ./cmd/ios-app-manager/.