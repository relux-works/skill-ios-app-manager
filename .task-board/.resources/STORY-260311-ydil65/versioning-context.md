Reference context from xflow-ios: root Project.swift defines marketingVersion/currentProjectVersion for the app target; Extensions/XFlowWidget/Project.swift duplicates those values for the widget extension; ios-app-manager.json also contains marketing_version/project_version metadata but is not enforced as runtime truth. Desired validation: regenerate the workspace and verify app plus widget emit identical CFBundleShortVersionString and CFBundleVersion, and add a check that fails if any target hardcodes divergent values.