# TASK-260302-1c7jkd: migrate-relux

## Description
Add register.go to internal/relux/ with init() + registry.Register(). Adapter SetupFromRegistry(). Stub Plan() and UsageGuide. Deps: [ioc]. Remove cli/relux.go manual command. Tests pass.

## Scope
(define task scope)

## Acceptance Criteria
(define acceptance criteria)
