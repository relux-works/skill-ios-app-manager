# TASK-260302-1ka5bq: impl-generic-setup-cmd

## Description
Implement generic NewSetupCommand() in internal/cli/ that generates cobra commands from registry Module metadata. Includes --yes, --dry-run flags, ExtraFlags support, two-phase flow (plan → confirm → setup). See design doc in STORY-260301-8hzuxt resources.

## Scope
(define task scope)

## Acceptance Criteria
(define acceptance criteria)
