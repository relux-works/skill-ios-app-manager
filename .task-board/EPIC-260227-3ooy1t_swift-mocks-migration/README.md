# EPIC-260227-3ooy1t: swift-mocks-migration

## Description
Migrate SwiftMocks (macro-based mocking framework) from GitHub org ivalxis to relux-works. Then add it as a Swift package dependency to generated projects (ios-app-manager init). SwiftMocks should be available for test targets that need it (UnitTests, IntegrationTests). Also add swift-testing-tools as a dependency for IntegrationTests target specifically.

## Scope
(define epic scope)

## Acceptance Criteria
(define acceptance criteria)
