# TASK-260224-1bejvy: dep-external

## Description
dep add/remove for external SPM packages. Add remote SPM dependency (URL + version). Updates root Package.swift or module Package.swift. dep list — shows all deps per module and project-wide.

## Scope
(define task scope)

## Acceptance Criteria
(define acceptance criteria)
