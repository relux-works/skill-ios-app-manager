# STORY-260224-1gmieb: agent-skill-foundation

## Description
Agent-facing skill built on top of the CLI. Follows agent-facing-api pattern: mini-query DSL for structured reads (field projection, batching) + scoped grep for full-text search + mutation DSL for writes. Skill SKILL.md documents all commands, DSL syntax, and usage patterns for AI agents. Minimizes token overhead.

## Scope
(define story scope)

## Acceptance Criteria
(define acceptance criteria)
