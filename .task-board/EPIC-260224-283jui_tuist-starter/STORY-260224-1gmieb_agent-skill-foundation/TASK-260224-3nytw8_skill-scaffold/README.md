# TASK-260224-3nytw8: skill-scaffold

## Description
Agent-facing skill scaffold: SKILL.md with frontmatter (name, description, triggers, globs), references/ directory structure. DSL query layer (q command) mapping to CLI queries with field projection. DSL mutation layer (m command) mapping to CLI mutations. Follow agent-facing-api pattern from skill.

## Scope
(define task scope)

## Acceptance Criteria
(define acceptance criteria)
