# TASK-260224-2n41dn: dsl-scaffold

## Description
Agent-facing-api DSL scaffold: q (query) and m (mutation) command handlers. Parser skeleton for DSL expressions like q summary(), m create(...). Follow agent-facing-api skill pattern. Wire into Cobra as top-level subcommands.

## Scope
(define task scope)

## Acceptance Criteria
(define acceptance criteria)
