# STORY-260224-3vkgqt: cli-skeleton

## Description
Scaffold the CLI tool in Go. Go module init, cobra/bubbletea or similar CLI framework. Agent-facing API pattern from agent-facing-api skill: q (query DSL) for reads, m (mutation DSL) for writes. Basic command structure. Entry point, help, version. Testing setup with go-testing-tools skill (tuitestkit, reducers, golden files).

## Scope
(define story scope)

## Acceptance Criteria
(define acceptance criteria)
