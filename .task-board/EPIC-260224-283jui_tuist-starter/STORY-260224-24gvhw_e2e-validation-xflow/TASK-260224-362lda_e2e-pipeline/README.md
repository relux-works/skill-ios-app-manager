# TASK-260224-362lda: e2e-pipeline

## Description
Full e2e pipeline test: feed xflow config → ios-app-manager init → scaffold project structure → create one demo Relux feature module TodoList (Interface: protocols + DTOs, Impl: Store + Reducer + State + Actions + SwiftUI View) → tuist generate → xcodebuild build. Must compile and build successfully. Smoke test for the entire toolchain.

## Scope
(define task scope)

## Acceptance Criteria
(define acceptance criteria)
