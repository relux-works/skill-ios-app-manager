# TASK-260224-1twvlw: root-commands

## Description
Root ios-app-manager command structure: config management (read/write/validate project config, multi-config support), status command (project overview — modules, deps, config health). Subcommand routing to tuist-proj-manager and relux-manager components.

## Scope
(define task scope)

## Acceptance Criteria
(define acceptance criteria)
