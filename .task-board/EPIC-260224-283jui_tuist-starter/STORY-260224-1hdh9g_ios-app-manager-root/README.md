# STORY-260224-1hdh9g: ios-app-manager-root

## Description
Root CLI component: ios-app-manager. Top-level entry point that orchestrates sub-components. Handles project-level operations: init, status, config. Routes to sub-components for modules, entitlements, dependencies, etc.

## Scope
(define story scope)

## Acceptance Criteria
(define acceptance criteria)
