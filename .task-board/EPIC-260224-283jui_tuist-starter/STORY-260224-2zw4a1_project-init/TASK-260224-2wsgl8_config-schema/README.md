# TASK-260224-2wsgl8: config-schema

## Description
Define project config JSON schema: APP_NAME, PROJECT_BUNDLE_ID, DEVELOPMENT_TEAM, APP_URL_SCHEME, app_groups[], MIN_TARGET_VERSION, SWIFT_VERSION, MARKETING_VERSION, CURRENT_PROJECT_VERSION, PRODUCT_NAME, ORGANIZATION_NAME. Parse + validate with clear errors. Support multiple config files per project (different flavors). Reference: .research/ref_membrana-config-example.json.

## Scope
(define task scope)

## Acceptance Criteria
(define acceptance criteria)
