# TASK-260224-vuf36c: init-command

## Description
Wire init command end-to-end: read config JSON → validate → render all Tuist templates → scaffold directory structure (Packages/, Targets/, Tuist/) → generate Makefile → generate .gitignore → generate base entitlements stub. Project must be ready for tuist generate after init.

## Scope
(define task scope)

## Acceptance Criteria
(define acceptance criteria)
