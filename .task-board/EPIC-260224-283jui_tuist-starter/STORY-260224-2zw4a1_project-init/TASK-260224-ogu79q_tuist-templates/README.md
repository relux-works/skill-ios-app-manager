# TASK-260224-ogu79q: tuist-templates

## Description
Tuist file templates with ${VARIABLE} substitution engine. Templates: Tuist.swift, Project.swift, Workspace.swift, root Package.swift. Template engine: read template → substitute ${VAR} from config → write. Reference: .research/ref_membrana-manifest-template.json for template pattern. Templates must produce valid Swift that Tuist can parse.

## Scope
(define task scope)

## Acceptance Criteria
(define acceptance criteria)
