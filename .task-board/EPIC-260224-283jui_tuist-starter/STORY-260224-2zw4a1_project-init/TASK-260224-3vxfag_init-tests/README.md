# TASK-260224-3vxfag: init-tests

## Description
Tests for project-init pipeline: config parsing (valid + invalid), template rendering (variable substitution), init command e2e (config → file system output). Golden file tests for generated Tuist files.

## Scope
(define task scope)

## Acceptance Criteria
(define acceptance criteria)
