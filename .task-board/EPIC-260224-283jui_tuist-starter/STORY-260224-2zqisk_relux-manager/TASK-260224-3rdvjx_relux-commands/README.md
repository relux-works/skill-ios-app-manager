# TASK-260224-3rdvjx: relux-commands

## Description
relux-manager CLI commands: init (bootstrap full Relux structure inside a module), add-action (add new action to Actions enum), add-middleware (add middleware template). All commands modify existing Swift files — must read, parse insertion points, write back.

## Scope
(define task scope)

## Acceptance Criteria
(define acceptance criteria)
