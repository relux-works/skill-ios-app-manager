# TASK-260224-agbaaz: module-types

## Description
Module type system: feature (full Relux + UI), kit (Relux, no UI), shared (shared state/services), UI (SwiftUI components, no Relux), utility (no Relux, no split). Each type maps to a template set and determines whether interface/impl split happens.

## Scope
(define task scope)

## Acceptance Criteria
(define acceptance criteria)
