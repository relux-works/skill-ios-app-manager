# TASK-260224-ng36rp: module-create

## Description
module create command: scaffolds interface+impl package pair for product modules (feature, kit, UI, shared), single package for utility modules. Calls tuist-proj-manager for Package.swift creation, then relux-manager for Relux boilerplate (product modules only). Module type determines template set.

## Scope
(define task scope)

## Acceptance Criteria
(define acceptance criteria)
