# TASK-260224-12c8wu: entitlements-crud

## Description
entitlements add/remove/list commands. Supported entitlements: push notifications, app groups, keychain sharing, iCloud, HealthKit, associated domains, background modes, etc. Modifies .entitlements plist file. Keeps Tuist project config in sync (entitlements path in target settings).

## Scope
(define task scope)

## Acceptance Criteria
(define acceptance criteria)
