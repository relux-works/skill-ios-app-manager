# TASK-260224-1u666i: push-token

## Description
Device token extraction: make push-token — launch app on simulator, parse simctl log stream output to catch token registration. Extract hex token. Output to stdout for piping to push-send. May need predicate filter for os_log subsystem.

## Scope
(define task scope)

## Acceptance Criteria
(define acceptance criteria)
