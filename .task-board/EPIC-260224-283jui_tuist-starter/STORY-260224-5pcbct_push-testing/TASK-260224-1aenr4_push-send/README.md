# TASK-260224-1aenr4: push-send

## Description
Parameterized APNS push send script: p8 JWT auth (ES256 via openssl), curl to api.development.push.apple.com or api.push.apple.com. All params from project config: key path, Key ID, Team ID, Bundle ID. Custom JSON payload support. make push-send ENV=dev/prod PAYLOAD=path TOKEN=device_token. Reference: .research/ref_send-push-with-p8.sh.

## Scope
(define task scope)

## Acceptance Criteria
(define acceptance criteria)
