# TASK-260224-1309fo: tuist-wrapper

## Description
Go wrapper around tuist CLI: generate, install, graph, clean, edit, dump. Exec tuist as subprocess, capture stdout/stderr, parse exit codes. Structured error handling. Tuist version detection and validation.

## Scope
(define task scope)

## Acceptance Criteria
(define acceptance criteria)
