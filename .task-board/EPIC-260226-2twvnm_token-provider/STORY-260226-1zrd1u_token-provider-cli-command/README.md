# STORY-260226-1zrd1u: token-provider-cli-command

## Description
Add a dedicated CLI command (e.g. ios-app-manager token-provider setup) that scaffolds the TokenProvider module into the project and auto-registers it in the IoC container. Similar to how relux setup and ioc setup work.

## Scope
(define story scope)

## Acceptance Criteria
(define acceptance criteria)
