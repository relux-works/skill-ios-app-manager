# STORY-260302-2bazl4: blueprint-ui-refactor

## Description
Refactor blueprint UI templates: Container split into extensions (content, buildProps, buildActions). Page split into extensions (content, states, components, reactions). Wire SwiftUIPlus + FoundationPlus deps correctly (FoundationPlus impl-only, SwiftUIPlus impl-only). Add @_exported imports in impl.

## Scope
(define story scope)

## Acceptance Criteria
(define acceptance criteria)
