# BUG-260623-10uv89: package-strictness-tuist-swift-version

## Description
Make generate package-strictness sync Swift language/build strictness into root Package.swift Tuist PackageSettings targetSettings for project-owned package targets, so Tuist-converted package targets do not drift back to -swift-version 5.

## Scope
(define bug scope / affected area)

## Acceptance Criteria
(define fix acceptance criteria)
