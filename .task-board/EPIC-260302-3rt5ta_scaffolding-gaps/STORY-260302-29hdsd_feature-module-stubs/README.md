# STORY-260302-29hdsd: feature-module-stubs

## Description
[A9 MEDIUM, Tier 1] Extend module create --type feature with optional stubs: --with-service generates Data/Api/<Name>Service.swift, --with-viewmodel generates UI/<Name>ViewModel.swift, --with-view generates UI/<Name>View.swift root view. For relux-feature type: all stubs included by default. Closes the gap between scaffolded skeleton and working feature — saves 100-200 lines of boilerplate per feature.

## Scope
(define story scope)

## Acceptance Criteria
(define acceptance criteria)
