# TASK-260302-2ufnu7: rework-entitlements-cli

## Description
Rework entitlements CLI commands. Current state: entitlements add/remove/list commands that manipulate plist files directly with 7 hardcoded aliases.

Changes:
1. Remove entitlements add command — capabilities are now declarative in Swift, not imperative CLI
2. Remove entitlements remove command — same reason
3. Keep entitlements list command — useful for reading/verifying current entitlements from plist
4. Remove GenerateEntitlements() from internal/scaffold/entitlements.go — entitlements plist is no longer generated by scaffold (EntitlementsFactory in Swift generates it at Tuist evaluation time)
5. Keep internal/entitlements/plist.go and manager.go List() function — still needed for list command
6. Remove or deprecate keys.go alias system — no longer needed for add/remove

Update CLI tests accordingly. The entitlements package itself (plist parsing) stays for reading capabilities.

Important: do NOT touch Project.swift template or scaffold pipeline — that is a separate task.

## Scope
(define task scope)

## Acceptance Criteria
(define acceptance criteria)
