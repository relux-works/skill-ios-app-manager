# STORY-260302-8ygetw: navigation-router

## Description
[A5+A14 MEDIUM, Tier 2] Scaffold AppRouter with NavigationPath, root destination enum (auth/main), deep link handler stub (.onOpenURL + URL scheme parsing), pending deep link support during auth. New router setup command or extend init. Covers ~150 lines of router infrastructure per project.

## Scope
(define story scope)

## Acceptance Criteria
(define acceptance criteria)
