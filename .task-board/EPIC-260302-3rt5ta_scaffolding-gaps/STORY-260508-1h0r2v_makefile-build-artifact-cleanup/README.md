# STORY-260508-1h0r2v: makefile-build-artifact-cleanup

## Description
Generated Makefile should expose reusable package-artifact cleanup and run Tuist generation before build/test so Xcode-generated package checkouts are restored after every agent build.

## Scope
(define story scope)

## Acceptance Criteria
(define acceptance criteria)
