# STORY-260302-2il43k: app-rename

## Description
[A10 MEDIUM, Tier 3] rename --from <old> --to <new> command. Updates: config file identifiers, bundle IDs, App Group IDs, Keychain groups, URL schemes, entitlements, shared package names, regenerates manifests. Infrequent operation but extremely painful when needed (~150 files touched in connect-ios rename).

## Scope
(define story scope)

## Acceptance Criteria
(define acceptance criteria)
