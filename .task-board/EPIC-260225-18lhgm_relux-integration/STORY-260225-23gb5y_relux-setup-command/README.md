# STORY-260225-23gb5y: relux-setup-command

## Description
Add relux setup CLI command that: 1) adds swift-relux and swiftui-relux as external deps in Package.swift and Project.swift, 2) updates Registry.swift with Relux-specific registrations (Relux, Relux.Store, Relux.Logger) and module registration via Relux result builder, 3) updates App.swift to use Relux.Resolver with splash/content/resolver pattern, 4) generates minimal SplashView.swift

## Scope
(define story scope)

## Acceptance Criteria
(define acceptance criteria)
