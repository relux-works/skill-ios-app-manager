# TASK-260714-18knpd: prepare-v0-10-1-release

## Description
Prepare the `v0.10.1` release for skill-ios-app-manager from the current `main` branch state.

## Scope
Summarize the release delta since `v0.10.0`, add `CHANGELOG.md`, verify the repository state, and publish the `v0.10.1` tag with matching GitHub release notes.

## Acceptance Criteria
- `CHANGELOG.md` includes `v0.10.1`.
- Validation for the release candidate is recorded.
- `v0.10.1` tag and GitHub release exist on the remote.
