# STORY-260309-3pon94: tuist-capabilities-integration

## Description
When entitlements change via CLI, auto-patch Tuist Project.swift to declare matching capabilities (e.g. .pushNotifications, .appGroups, .healthKit, .backgroundModes). Ensure generated project has correct capability declarations that match the entitlements plist. Handle the mapping: entitlement key → Tuist capability enum case.

## Scope
(define story scope)

## Acceptance Criteria
(define acceptance criteria)
