# STORY-260309-eckfsw: entitlements-add-remove

## Description
Implement entitlements add and entitlements remove CLI subcommands. Add key with optional value (string, bool, string-array). Remove key by name. Auto-detect plist path from config or accept explicit --path flag. Support common aliases: push, app-groups, keychain, icloud, healthkit, associated-domains, background-modes. Validate values where applicable (e.g. aps-environment must be development or production).

## Scope
(define story scope)

## Acceptance Criteria
(define acceptance criteria)
