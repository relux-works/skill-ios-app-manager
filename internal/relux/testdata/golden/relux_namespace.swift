public enum Notes {
    public enum Data {
        public enum Api {
            public enum DTO {}
        }
    }
    public enum Business {
        public enum Model {}
    }
    public enum UI {
        public enum Model {}
    }
}
