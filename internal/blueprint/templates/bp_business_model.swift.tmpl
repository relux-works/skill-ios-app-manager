import Foundation

extension {{.ModuleName}}.Business.Model {
    /// Scaffolded example domain model.
    /// Replace with your real domain models.
    public struct Scaffolded: Identifiable, Equatable, Sendable {
        public let id: UUID
        public let title: String

        public init(id: UUID, title: String) {
            self.id = id
            self.title = title
        }
    }
}
