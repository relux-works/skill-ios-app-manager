import Foundation

extension {{.ModuleName}}.Data.Api.DTO {
    /// Scaffolded example DTO response.
    /// Replace with your real API response models.
    public struct ScaffoldedResponse: Codable, Sendable {
        public let id: UUID
        public let title: String

        public init(id: UUID, title: String) {
            self.id = id
            self.title = title
        }
    }
}
