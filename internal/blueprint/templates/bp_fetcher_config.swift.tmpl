import Foundation

extension {{.ModuleName}}.Data.Api.Http {
    public final class Config: Sendable {
        /// Scaffolded example endpoint.
        /// Replace with your real API endpoint configuration.
        public let scaffoldedEndpoint: URL

        public init(
            scaffoldedEndpoint: URL = URL(string: "https://api.example.com/scaffolded")!
        ) {
            self.scaffoldedEndpoint = scaffoldedEndpoint
        }
    }
}
